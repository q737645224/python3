# 3. 有一些数存于列表中:
#   L = [1, 3, 2, 1, 6, 4, 2, ..... 98, 82]
#   将列表中出现的数字存入到另一个列表L2中
#     要求: 重复出现多次的数字在L2列表中只保留一份(去重)


L = [1, 3, 2, 1, 6, 4, 2, 6, 8, 5, 3, 98, 82]

L2 = []
for x in L:
    if x not in L2:  # L2里没有这个数
        L2.append(x)

print(L2)

