# 2. 生成前40个斐波那契数(Fibonacci)
#     1  1  2  3  5  8  13  .....
#     要求将这些数保存在列表中
#     打印这些数

# 方法1
# a = 0  # a 代表当前数的前1个
# b = 1  # b代表当前数

# L = []  # 用来存放算出来的数
# while len(L) < 40:
#     # 此处拿到斐波那契数，放入列表中
#     L.append(b)
#     # 算出下次循环需要的数
#     # c = a + b
#     # a = b
#     # b = c
#     a, b = b, a + b  # 序列赋值

# 方法2
L = [1, 1]
while len(L) < 40:
    L.append(L[-1] + L[-2])

print(L)
